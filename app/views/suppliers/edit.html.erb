<div>
	<%= render(:partial => "sub_menu")%>
	<h2>Edit Supplier Info</h2>
	
	<%= form_for @supplier, :url => {:action => 'update', :id => @supplier.id} do |f| %>
	
	<%= render(:partial => "new_form", :locals => {:f => f})%>
	
	<div class="form-buttons">
		<%= submit_tag("Update") %>
	</div>
	
	<% end %>
	
</div>
