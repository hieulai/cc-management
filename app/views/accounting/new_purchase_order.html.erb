<div>
    <%= render(:partial => "layouts/accounting_sub_menu")%>
    <h2>Add Purchase Order to Project <%= @project.name %></h2>

    <%= form_for @purchase_order, :url => {:action => 'create_purchase_order', :id => @project.id} do |f| %>

      <% if @purchase_order.errors.any? %>
        <div id="errorExplanation">
            <h2><%= pluralize(@purchase_order.errors.count, "Error") %> prohibited
                this post from being saved:</h2>
            <ul>
                <% @purchase_order.errors.full_messages.each do |msg| %>
                  <li><%= msg %></li>
                <% end %>
            </ul>
        </div>
      <% end %>
      <%= render(:partial => "purchase_order_form", :locals => {:f => f, :project => @project})%>
      <div id="item-placeholder">
          <%= render(:partial => "purchase_order_items", :locals => {:purchase_order => @purchase_order, :categories_template => @purchase_order.categories_template})%>
      </div>

    <% end %>

    <div class="form-buttons">
        <%= submit_tag("Create", :data => {:form => ".new_purchase_order"}) %>
    </div>

</div>