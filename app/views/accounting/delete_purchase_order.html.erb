<div>
    <%= render(:partial => "layouts/accounting_sub_menu")%>
    <h2>Delete Purchase Order</h2>

    <%= form_for(:purchase_order, :url => {:action => 'destroy_purchase_order', :id => @purchase_order.id}) do |f| %>
      <%= render :partial => 'shared/errors', :locals => {:obj => @purchase_order} %>

      <p>Are you sure you want to delete this purchase order?</p>

      <div class="form-buttons">
          <%= submit_tag("Delete", class: 'btn') %>
      </div>

    <% end %>
</div>