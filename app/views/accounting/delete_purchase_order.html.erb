<div>
    <%= render(:partial => "layouts/accounting_sub_menu")%>
    <h2>Delete Purchase Order</h2>

    <%= form_for(:vendor, :url => {:action => 'destroy_purchase_order', :id => @purchase_order.id}) do |f| %>

      <p>Are you sure you want to delete this purchase order?</p>

      <div class="form-buttons">
          <%= submit_tag("Delete") %>
      </div>

    <% end %>
</div>