<tr class="purchased">
    <td class="center"><%= link_to raw("<button type=\"button\">-</button>"), "#", :class => 'remove-item' %></td>
    <td>
        <%= item.name %>
        <%= hidden_field_tag 'items[][name]', item.name %>
    </td>
    <td>
        <%= item.description %>
        <%= hidden_field_tag 'items[][description]', item.description %>
    </td>
    <td class="right">
        <%= text_field_tag 'items[][qty]', item.qty, :class => "text-field", :size => 10 %>
    </td>
    <td class="right">
        $<%= text_field_tag 'items[][estimated_cost]', item.estimated_cost(true), :class => "text-field", :size => 10 %>
    </td>
    <td class="right light-blue">
        <div class="actual-amount-placeholder">
            <%= price_tag item.amount(true), 'actual-amount' %>
        </div>
        <%= hidden_field_tag 'items[][actual_cost]', item.amount(true) %>
    </td>
    <td class="right">
        <%= price_tag 0 %>
    </td>
</tr>