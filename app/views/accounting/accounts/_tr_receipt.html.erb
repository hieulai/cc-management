<tr class="clickable <% if t.reconciled%> unclickable <% end %>">
    <td class="center">
        <%= t.date %>
        <%= link_to("Edit", {:controller => 'accounting', :action => 'edit_receipt', :id => t.transactionable_id}, :class => 'action edit clickable-link', :style => 'display: none') %>
    </td>
    <td>Receipt</td>
    <td class="center"><%= t.transactionable.reference %></td>
    <td></td>
    <td class="right"><%= price_tag(t.people_amount(project_id)) %></td>
    <td class="right"><%= price_tag(balance) %></td>
</tr>