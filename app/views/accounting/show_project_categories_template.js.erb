$('#categories_template-placeholder').html('<%= escape_javascript(select(:purchase_order, :categories_template_id, @project.estimates.any? ? @project.estimates.first.template.categories_templates.sort_by { |c| c.category.name }.map { |c| [c.category.name, c.id] } : Array.new, {:include_blank => true}, :data => {:remote => true, :url => url_for(:action => "show_categories_template_items", :id => @purchase_order.id)})) %>');
$('select[name="purchase_order[categories_template_id]"]').change();