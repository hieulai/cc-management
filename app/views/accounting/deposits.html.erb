<div>
    <%= render(:partial => "layouts/accounting_sub_menu") %>
    <div class="zurb-row main-action-bar">
        <div class="large-3 medium-4 columns">
            <%= link_to("+ Deposit", {:action => 'new_deposit'}, :id => 'add-deposit', :class => 'add action-button') %>
            <span class="trigger help action-button">Instructions >></span>

            <div class="instructions">
                <p>Click on the row of the deposit you want to edit their information.</p>
            </div>
        </div>
        <div class="large-6 medium-6 columns">
            <h1>Deposits</h1>
        </div>
        <div class="large-3 medium-2 columns">
        </div>
    </div>
    <div class="zurb-row">
        <div class="columns margin-bottom">
            <div class="button-group">
                <%= form_tag('deposits', :method => 'get', :class => 'form-inline') do %>
                  <p>
                      <%= label_tag :query, "Search" %>
                      <%= text_field_tag(:query, @query) %>
                  </p>
                <% end %>
            </div>
            <%= render(:partial => "deposits", :locals => {:deposits => @deposits}) %>
        </div>
    </div>
</div>

