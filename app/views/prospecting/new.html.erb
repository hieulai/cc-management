<div>
    <%= render(:partial => "layouts/prospecting_sub_menu") %>
    <div class="zurb-row">
        <div class="columns">
            <h2>Add Prospect</h2>
            <%= form_for @prospect, :url => {:action => 'create'}, :html => {:class => 'form-horizontal'} do |f| %>
                <% if @prospect.errors.any? %>
                    <div id="errorExplanation">
                        <h2><%= pluralize(@prospect.errors.count, "Error") %> prohibited this prospecting from being
                            saved:</h2>
                        <ul>
                            <% @prospect.errors.full_messages.each do |msg| %>
                                <li><%= msg %></li>
                            <% end %>
                        </ul>
                    </div>
                <% end %>
                <%= render(:partial => "form", :locals => {:f => f}) %>
                <div class="control-group">
                    <div class='controls'>
                        <%= submit_tag("Create", :class => 'btn') %>
                    </div>
                </div>
            <% end %>
        </div>
    </div>
</div>