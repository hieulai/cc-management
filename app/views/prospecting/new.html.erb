<div>
	<%= render(:partial => "layouts/prospecting_sub_menu")%>
	<h2>Add Prospect</h2>
	
	<%= form_for @prospect, :url => {:action => 'create'} do |f| %>
	
	<% if @prospect.errors.any? %>
	  <div id="errorExplanation">
	    <h2><%= pluralize(@prospect.errors.count, "Error") %> prohibited
	      this post from being saved:</h2>
	    <ul>
	    <% @prospect.errors.full_messages.each do |msg| %>
	      <li><%= msg %></li>
	    <% end %>
	    </ul>
	  </div>
	<% end %>
	
	<%= render(:partial => "form", :locals => {:f => f})%>
	
	<div class="form-buttons">
		<%= submit_tag("Create") %>
	</div>
	
	<% end %>
	
</div>