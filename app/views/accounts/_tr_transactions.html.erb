<% transactions.each do |transaction| %>
    <%= render(:partial => "tr_#{transaction.transactionable_type.underscore}", :locals => {:t => transaction, :balance => @balance, :account => @account}) %>
    <% @balance -= transaction.amount.to_f %>
<% end %>
