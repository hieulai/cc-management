<% transactions.each do |transaction| %>
    <%= render(:partial => "tr_#{transaction.class.name.underscore}", :locals => {:t => transaction, :balance => @balance, :account => @account}) %>
    <% @balance -= transaction.account_amount.to_f %>
<% end %>
