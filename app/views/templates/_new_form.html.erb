<p>
  <%= f.label(:name, 'Template Name') %>
	<%= f.text_field(:name) %>
  <span id="alertError" style="display:none;">this field can't be empty</span>
</p>
<%= f.fields_for :categories_templates do |ct| %>
  <%= render('categories', f: ct) %>
<% end %>
<p style="margin-left: 40px"><%= link_to_add_association 'Add Category', f, :categories, partial: 'categories' %></p>