<p>
	Template Name
	<%= f.text_field(:name) %>
</p>

<%= f.fields_for :categories do |builder| %>
	<%= render 'categories', :f => builder %>
<% end %>

<p style="margin-left: 40px"><%= link_to_add_fields "Add Category", f, :categories %></p>

	