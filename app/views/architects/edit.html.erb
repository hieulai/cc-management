<div>
	<%= render(:partial => "sub_menu")%>
	<h2>Edit Architect's Info</h2>
	
	<%= form_for @architect, :url => {:action => 'update', :id => @architect.id} do |f| %>
	
	<%= render(:partial => "new_form", :locals => {:f => f})%>
	
	<div class="form-buttons">
		<%= submit_tag("Update") %>
	</div>
	
	<% end %>
	
</div>
