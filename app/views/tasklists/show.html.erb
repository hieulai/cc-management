<div>
	<%= render(:partial => "layouts/project_mgmt_sub_menu")%>
	
	<h2>Details For: <%= @tasklist.name %> </h2>
	<p>
		<%= "Name:" %>
		<%= @tasklist.name %>
	</p>
	<% for task in @tasklist.tasks %>
	<li><%= task.name %></li>
	<% end %>
	
</div>