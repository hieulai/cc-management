<div>
    <%= render(:partial => "layouts/project_mgmt_sub_menu") %>
    <div class="zurb-row">
        <div class="columns">
            <h2>Add New Specification</h2>
            <%= form_for @specification, :method => :post, :html => {:class => 'form-horizontal'} do |f| %>
                <% if @specification.errors.any? %>
                    <div id="errorExplanation">
                        <h2><%= pluralize(@specification.errors.count, "Error") %> prohibited
                            this specification from being saved:</h2>
                        <ul>
                            <% @specification.errors.full_messages.each do |msg| %>
                                <li><%= msg %></li>
                            <% end %>
                        </ul>
                    </div>
                <% end %>
                <%= render(:partial => "form", :locals => {:f => f}) %>
                <div class="control-group">
                    <div class='controls'>
                        <%= submit_tag("Create", :class => 'btn') %>
                    </div>
                </div>
            <% end %>
        </div>
    </div>
</div>