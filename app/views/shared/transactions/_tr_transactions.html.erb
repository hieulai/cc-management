<% transactions.each do |transaction| %>
  <%= render(:partial => "shared/transactions/tr_#{transaction.transactionable_type.underscore}", :locals => {:t => transaction, :balance => balance}) %>
    <% balance -= transaction.amount.to_f %>
<% end %>
