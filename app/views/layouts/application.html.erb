<!DOCTYPE html>
<html>
<head>
    <title>Construction Central</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <%= stylesheet_link_tag "application", :media => "all" %>
    <%= stylesheet_link_tag "private", :media => "all" %>
    <%= javascript_include_tag :application %>
    <%= csrf_meta_tags %>
</head>
<body>
<div class='zurb-row' id="page">
    <div class='zurb-row' id="header">
        <div class="large-9 medium-9 small-12 columns">
            <h1>Construction Central<div class="loader inline-block" style="display: none"></div></h1>

        </div>
        <div class="large-3 medium-3 small-12 columns">
            <div id='username'>
                <%= (session[:builder_name]) if session[:builder_name] %>
                </br>
                <%= (session[:username]) if session[:username] %> <%= link_to("Logout", destroy_user_session_url, :method => :delete, :class => "logout") %>
            </div>
        </div>
    </div>
    <%= render :partial => 'layouts/navigation' %>
    <div class='zurb-row' id='content'>
        <%= yield %>
    </div>
</div>
<%= yield :javascript %>
</body>
</html>

