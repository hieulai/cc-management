<div>
	<%= render(:partial => "lead_sub_menu")%>
	<h2>Update Lead</h2>
	<%= form_for @project, :url => {:action => 'update', :id => @project.id} do |p| %>
	<%= fields_for @client do |c| %>
	
	
	<%= render(:partial => "lead_form", :locals => {:c => c, :p => p })%>
	
	<div class="form-buttons">
		<%= submit_tag("Update Lead") %>
	</div>

	<% end %>
	<% end %>
</div>