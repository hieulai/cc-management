<div>
	<%= render(:partial => "lead_sub_menu")%>
	<h1>Current Leads</h1>
	<%= link_to("Instructions >>", {:action => 'show'}) %>
	</br>
	</br>
	<%= link_to("Add New Lead >>", {:action => 'select_client'}) %>
	</br>
	</br>
	<table class="grid-view" summary="Lead List">
		<tr class="header">
			<th>Actions</th>
			<th>Lead Stage</th>
			<th>Project Name</th>
			<th>Last Contacted</th>
			<th>Client Name</th>
			<th>Primary Phone</th>
			<th>Email</th>
			<th>Project Type</th>
			<th>Exp. Revenue</th>
			<th>Lead Source</th>
			<th>Notes</th>
			
		</tr>
		<% @clients.each do |client| %>
		<% client.projects.each do |lead| %>
		
		<tr>
			<td class="center">
				<%= link_to("View", {:action => 'show', :id => lead.id}, :class => 'action vew') %>
				<%= link_to("Edit", {:action => 'edit', :id => lead.id}, :class => 'action edit') %>
				<%= link_to("Convert", {:action => 'convert', :id => lead.id}, :class => 'action convert') %>
				<%= link_to("Delete", {:action => 'delete', :id => lead.id}, :class => 'action delete') %>
			</td>
			<td><%= lead.lead_stage %></td>
			<td><%= lead.name %></td>
			<td class="center"><%= client.last_contacted %></td>
			<td><%= client.full_name %></td>
			<td><%= client.primary_phone %></td>
			<td><%= client.email %></td>
			<td><%= lead.project_type %></td>
			<td><%= lead.revenue%></td>
			<td><%= client.lead_source %></td>
			<td><%= lead.lead_notes%></td>
		</tr>
		<% end %>
		<% end %>
	</table>
</div>
<!-- @clients.each do {|client| client.projects.each do {|project| project.name}}
  -->
