<div>
	<%= render(:partial => "layouts/people_sub_menu")%>
	<h2>Add Client</h2>
	
	<%= form_for @client, :url => {:action => 'create'}, html: {class: 'form-horizontal'} do |f| %>
	
	<% if @vendor.errors.any? %>
	  <div id="errorExplanation">
	    <h2><%= pluralize(@vendor.errors.count, "Error") %> prohibited
	      this client from being saved:</h2>
	    <ul>
	    <% @vendor.errors.full_messages.each do |msg| %>
	      <li><%= msg %></li>
	    <% end %>
	    </ul>
	  </div>
	<% end %>
	
	<%= render(:partial => "form", :locals => {:f => f})%>

      <div class="control-group">
          <div class='controls'>
              <%= submit_tag("Create", class: 'btn') %>
          </div>
      </div>
	
	<% end %>
	
</div>